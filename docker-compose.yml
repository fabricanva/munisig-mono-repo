
services:
  db:
    image: postgis/postgis:15-3.3
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
      POSTGRES_DB: gis_db
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
